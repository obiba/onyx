<list>
  <purge>
    <valueset entityType="Participant">
      <entities>
      
        <excludeAll />
        
        <script type="INCLUDE">
          <javascript>
             // If the interview has been exported AND the interview was finished more than 'org.obiba.onyx.participant.purge' days ago, then purge.
             ($('Admin.Interview.exportLog.destination').empty().not()).and(now().after($('Admin.Interview.endDate').add(onyx('org.obiba.onyx.participant.purge'))))  
          </javascript>
        </script>
    
        <script type="INCLUDE">
          <javascript>
             // If the interview has been 'CLOSED' or 'CANCELED' AND the interview was finished prior to the last export, then purge.
             ($('Admin.Interview.endDate').isNull().not()).and(onyx('org.obiba.onyx.lastExportDate').after($('Admin.Interview.endDate'))).and($('Admin.Interview.status').any('CLOSED','CANCELED'))
          </javascript>
        </script>
    
      </entities>
    </valueset>
  </purge>
</list>
