<!-- 
  This is required to hot-deploy under Windows. Windows has a tendency to lock jar files 
  when accessing files using URLs in Java code. Which is something we do often.
-->
<Context antiResourceLocking="true" antiJARLocking="true"></Context>
